[Service]
# Chave AES-256 do armazenamento processual, entregue em tmpfs pelo systemd.
# O blob deve ser provisionado fora do repositório com o nome abaixo.
LoadCredentialEncrypted=audit_master_key:/etc/credstore.encrypted/pjepa-mcp/audit_master_key.cred
