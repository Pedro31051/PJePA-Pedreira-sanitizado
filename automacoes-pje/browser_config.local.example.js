"use strict";

// Copie para browser_config.local.js e ajuste para o módulo compartilhado.
// O arquivo local é ignorado pelo Git e não deve conter credenciais.
module.exports = require("/caminho/portatil/para/browser_config.js");
